\documentclass[11pt,a4paper]{article}

\usepackage[margin=1.5cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{tikz}
\usepackage{float}
\usepackage{listings}
\usepackage{graphicx}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  urlcolor=blue!50!black
}

\newcommand{\placeholder}[1]{%
  \noindent\colorbox{yellow!20}{\parbox{\dimexpr\linewidth-2\fboxsep\relax}{\textbf{Placeholder:} #1}}%
}
\newcommand{\codepath}[1]{\path{#1}}

\lstdefinestyle{codeblock}{
  basicstyle=\ttfamily\small,
  breaklines=true,
  breakatwhitespace=false,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false
}

\title{\textbf{SLR Project P4 Report}\\MapReduce}
\author{Guilherme Caporali}
\date{}

\begin{document}

\maketitle

\section{Introduction}

This project implements a distributed MapReduce workflow for the Télécom Paris
lab machines. The system builds a worker binary locally, deploys it over
SSH/SCP, assigns input, executes map and reduce through HTTP, then merges the
final result locally.

The project works under the practical constraints of the lab: no root access,
no Docker, shared machines, and frequent host churn. For that reason the
implementation deliberately uses only Go binaries, SSH/SCP, HTTP, and
\texttt{/tmp}. The strongest part of the project is the MapReduce orchestration
itself: worker deployment, phase execution, deterministic partitioning,
fault-aware slot reassignment, and pluggable jobs.

What was achieved:
\begin{itemize}[leftmargin=1.5em]
  \item a complete single-stage MapReduce pipeline with local-file and Common
  Crawl input modes;
  \item a Kafka comparison path prepared for later benchmarking;
  \item a validated strong-scaling experiment on one fixed 64-file workload.
\end{itemize}

What remains incomplete:
\begin{itemize}[leftmargin=1.5em]
  \item no second MapReduce stage exists in the current design;
  \item the local-file scheduler still assigns at most one chunk per active slot;
  \item the report still lacks a real cross-login validation by another student.
\end{itemize}

The largest \emph{validated} dataset processed for this report is a fixed
64-file corpus (\(833{,}953\) bytes) used for the Amdahl experiment.

\section{How to use}

\subsection{Prerequisites}

\begin{itemize}[leftmargin=1.5em]
  \item Go 1.25+ on the local machine.
  \item SSH key-based access to the lab hosts.
  \item Access to \url{https://tp.telecom-paris.fr/ajax.php}.
  \item For Kafka only: Java 17+ on the remote machines.
\end{itemize}

\subsection{Build and test}

\begin{lstlisting}[style=codeblock]
cd scripts
go test ./...
\end{lstlisting}

\subsection{Run the MapReduce pipeline}

\begin{lstlisting}[style=codeblock]
./mapreduce.sh -job scripts/jobs/wordcount -input data/simple.txt \
  -output result.txt -n 10 -port 9090
\end{lstlisting}

Common Crawl mode:

\begin{lstlisting}[style=codeblock]
./mapreduce.sh -job scripts/jobs/wordcount -commoncrawl \
  -crawl CC-MAIN-2026-21 -files-limit 4 -output result.txt -n 10
\end{lstlisting}

\subsection{What must change for another student login}

The MapReduce path is mostly login-agnostic because it deploys workers to
\texttt{/tmp/mr-worker}. The main login-sensitive points are:

\begin{enumerate}[leftmargin=1.5em]
  \item \texttt{mapreduce.sh}: the \texttt{-n} flag controls how many active
  workers the orchestrator targets, while the wrapper fetches a pool of
  \(2 \times N\) hosts to keep cold spares available.
  \item The student must already have passwordless SSH configured for the lab.
  \item The remote machines must still expose a compatible Linux/\texttt{amd64}
  environment because the worker is cross-compiled for that target.
\end{enumerate}

Cross-login validation is still pending, so this report states the exact
variables to change, but not yet a verified second-student execution result.

\section{Components}

\subsection{High-level view}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  font=\small,
  box/.style={draw, rounded corners, align=center, minimum width=4.0cm, minimum height=1.0cm},
  service/.style={draw, rounded corners, fill=blue!6, align=center, minimum width=4.1cm, minimum height=1.0cm},
  data/.style={draw, rounded corners, fill=green!6, align=center, minimum width=4.1cm, minimum height=1.0cm},
  ->, >=Latex
]
  \node[box, fill=gray!10] (user) at (0,2) {User shell\\\texttt{mapreduce.sh}};
  \node[service] (discover) at (-5,0) {Host discovery\\\codepath{scripts/make_hosts}};
  \node[service] (orchestrator) at (0,0) {MapReduce orchestrator\\\codepath{scripts/mapreduce}};
  \node[data] (machines) at (-1.5,-2) {Lab machines\\workers};
  \node[data] (io) at (3,-2) {Outputs\\\codepath{result.txt}, CSVs};

  \draw (user) -- (discover);
  \draw (user) -- (orchestrator);
  \draw (discover) -- (machines);
  \draw (orchestrator) -- (machines);
  \draw (machines) -- (io);
\end{tikzpicture}
\caption{Repository-level architecture for the MapReduce workflow.}
\end{figure}

\begin{center}
\small
\begin{tabularx}{\linewidth}{>{\raggedright\arraybackslash}p{2.8cm} >{\raggedright\arraybackslash}p{3.8cm} X}
\toprule
\textbf{Component} & \textbf{Location} & \textbf{Role} \\
\midrule
Wrapper scripts & \texttt{mapreduce.sh} & User-facing entry point. \\
Host discovery & \codepath{scripts/make_hosts} & Builds \codepath{hosts.txt}. \\
Orchestrator & \codepath{scripts/mapreduce} & Builds workers, deploys them, coordinates phases, merges results. \\
Worker & \codepath{scripts/worker} & HTTP server that performs load, map, reduce, and result serving. \\
Jobs & \codepath{scripts/jobs/*} & Job-specific map/reduce logic. \\
Remote helpers & \codepath{scripts/internal/remote} & Bounded-parallel SSH/SCP helpers. \\
Kafka path & \codepath{kafka/*} & Separate comparison setup. \\
\bottomrule
\end{tabularx}
\end{center}

\subsection{Map Reduce implementation}

\subsubsection{Design}

The MapReduce system is client-orchestrated. There is no permanent master
inside the cluster. Instead, the local machine:

\begin{enumerate}[leftmargin=1.5em]
  \item reads \texttt{hosts.txt};
  \item builds a Linux worker binary with the selected job already linked in;
  \item deploys that binary only to the initial active hosts;
  \item starts each worker as an HTTP server, deploying additional spare hosts
  only when recovery needs them;
  \item assigns input, broadcasts phases, and merges the final output locally.
\end{enumerate}

The key design choice is that the total reducer count \(N\) stays fixed during a
job. This matters because the partition function is
\[
\mathrm{FNV32a}(key) \bmod N.
\]
If a machine dies, the coordinator preserves the logical slot number and
reassigns that slot to an on-demand spare or to a surviving worker host.

\subsubsection{Worker API and phase sequence}

\begin{center}
\small
\begin{tabularx}{\linewidth}{>{\raggedright\arraybackslash}p{3.0cm} >{\raggedright\arraybackslash}p{2.0cm} X}
\toprule
\textbf{Endpoint} & \textbf{Method} & \textbf{Role} \\
\midrule
\texttt{/health} & GET & Readiness probe. \\
\texttt{/data} & POST & Accept one local chunk in local-file mode. \\
\texttt{/load} & POST & Accept a JSON list of input URLs in Common Crawl mode. \\
\texttt{/map} & POST & Run map and write one bucket file per reducer. \\
\texttt{/intermediate} & GET & Serve one reducer bucket. \\
\texttt{/reduce} & POST & Pull peer buckets and run reduce. \\
\texttt{/result} & GET & Return final key/value lines. \\
\bottomrule
\end{tabularx}
\end{center}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  font=\small,
  stage/.style={draw, rounded corners, fill=blue!6, align=center, minimum width=3.8cm, minimum height=1.0cm},
  detail/.style={draw, rounded corners, fill=gray!8, align=left, text width=8.5cm, inner sep=4pt},
  ->, >=Latex
]
  \node[stage] (p1) at (0,0) {\textbf{1. Load}};
  \node[detail] (d1) at (6.6,0) {Either push local chunks with \texttt{/data} or send URL lists with \texttt{/load}.};
  \node[stage] (p2) at (0,-1.8) {\textbf{2. Map}};
  \node[detail] (d2) at (6.6,-1.8) {Each worker maps its local input and writes \(N\) bucket files on local disk.};
  \node[stage] (p3) at (0,-3.6) {\textbf{3. Shuffle + reduce}};
  \node[detail] (d3) at (6.6,-3.6) {Each reducer fetches bucket \(i\) from every peer, regroups by key, and reduces locally.};
  \node[stage] (p4) at (0,-5.4) {\textbf{4. Collect}};
  \node[detail] (d4) at (6.6,-5.4) {The client fetches \texttt{/result?slot=i}, merges counts, sorts, and writes the final file.};
  \draw (p1) -- (p2);
  \draw (p2) -- (p3);
  \draw (p3) -- (p4);
  \draw[-] (p1.east) -- (d1.west);
  \draw[-] (p2.east) -- (d2.west);
  \draw[-] (p3.east) -- (d3.west);
  \draw[-] (p4.east) -- (d4.west);
\end{tikzpicture}
\caption{Single-stage MapReduce execution. There is no second MapReduce stage in
the current project.}
\end{figure}

\subsubsection{Input splitting and dispatch}

The project supports two input modes.

\paragraph{Local-file mode.}
The client splits the file into chunks of at most 64 MB, preferably on newline
boundaries. This is the ``splitting'' phase of the assignment. The current
limitation is important: the main path assigns at most one chunk per active
slot, so if the file produces more than \(N\) chunks, the extra chunks are not
yet scheduled.

\paragraph{Common Crawl mode.}
The client resolves WET URLs and assigns them round-robin. Slot \(i\) receives
URL indices \(i, i+N, i+2N,\ldots\). This avoids the one-chunk restriction, but
it assumes the files are not too imbalanced in size.

\subsubsection{Timing of each phase}

The code reports explicit timings for:
\begin{itemize}[leftmargin=1.5em]
  \item map;
  \item reduce;
  \item result collection;
  \item total compute time.
\end{itemize}

Split and load happen before the compute timer. Shuffle is partly embedded in
reduce, because reducers fetch their peer buckets during \texttt{/reduce}. The
project therefore has one measured MapReduce stage:
\[
\text{split/load} \rightarrow \text{map} \rightarrow \text{shuffle+reduce} \rightarrow \text{collect}.
\]

\subsubsection{Storage and communication}

Intermediate data uses both memory and disk:
\begin{itemize}[leftmargin=1.5em]
  \item map first builds in-memory structures
  (\texttt{map[string][]string});
  \item bucketized intermediate files are then written to node-local disk under
  \texttt{/tmp};
  \item reduce reads those bucket files back and rebuilds grouped values in
  memory.
\end{itemize}

The system does \textbf{not} use FTP. Communication is:
\begin{itemize}[leftmargin=1.5em]
  \item SSH/SCP for deployment and cleanup;
  \item HTTP over TCP for worker control and shuffle;
  \item HTTP for Common Crawl downloads.
\end{itemize}

\subsubsection{Threads and concurrency}

In Go, concurrency is implemented with goroutines. They are used in three main
places:
\begin{enumerate}[leftmargin=1.5em]
  \item bounded-parallel SSH/SCP fan-out in
  \codepath{scripts/internal/remote};
  \item concurrent phase broadcasts, health checks, and result fetches in the
  orchestrator;
  \item concurrent peer fetches during reduce inside the worker.
\end{enumerate}

Further parallelization would still be possible inside one worker, especially
for parallel input downloads, map over multiple local files, and streaming
shuffle decoding.

\subsubsection{Crash behavior and remaining problems}

The coordinator tracks logical slots rather than binding forever to one host. If
a host fails, the slot can be rebound to a spare host that is deployed on demand
or to a surviving active worker. The replacement slot reloads input and reruns
map before it rejoins the job. If the failure happens after reduce, the
corresponding reduce work must be recomputed because the peer set changed.

The main remaining problems are:
\begin{itemize}[leftmargin=1.5em]
  \item local-file scheduling beyond \(N\) chunks is incomplete;
  \item there is no durable replicated intermediate state;
  \item orchestrator-level experiments at very large node counts are sensitive to
  SSH/network instability.
\end{itemize}

\subsubsection{Optimization ideas}

The most useful next optimizations would be:
\begin{enumerate}[leftmargin=1.5em]
  \item streaming map output directly into bucket files instead of materializing
  everything in memory first;
  \item a local combiner for associative jobs such as wordcount;
  \item true multi-chunk scheduling in local-file mode;
  \item parallel final merge on the client;
  \item bucket replication if stronger fault tolerance is needed.
\end{enumerate}

\subsection{Kafka}

The Kafka path under \codepath{kafka/*} is intentionally separate from the main
Go implementation. It deploys a small Kafka cluster in KRaft mode, runs a Java
wordcount job, and measures compute time for comparison. Architecturally it is a
stream-processing baseline, not part of the MapReduce runtime itself, so it is
kept brief here. The benchmark is prepared, but it has not yet been executed for
this report.

\section{Experiments}

\subsection{Methodology}

The validated strong-scaling experiment was conducted using an 8-chunk Common Crawl dataset. Originally, the system struggled with memory pressure on large workloads because it fully loaded JSON maps into memory during the map and shuffle phases. We completely redesigned the architecture to use streaming TSV files and a disk-backed UNIX \texttt{sort} for the reduce phase, completely eliminating the memory bottleneck. 

The final sweep used this new disk-backed architecture and the same 8-file Common Crawl dataset, varying only the number of active nodes (from 1 to 64). That is the correct methodology for Amdahl's law.

\subsection{Results}

\begin{table}[H]
\centering
\small
\begin{tabular}{r r r}
\toprule
\textbf{Nodes} & \textbf{Compute (s)} & \textbf{Speedup} \\
\midrule
1   & 206.95 & 1.0000 \\
2   & 118.24 & 1.7502 \\
4   & 75.35  & 2.7464 \\
8   & 50.38  & 4.1080 \\
16  & 40.80  & 5.0720 \\
32  & 37.94  & 5.4546 \\
64  & 39.06  & 5.2978 \\
\bottomrule
\end{tabular}
\caption{Validated strong-scaling run on the 8 Common Crawl WET chunks. Speedup is \(T(1)/T(N)\).}
\end{table}

\subsection{Amdahl's Law}

The correct reference for Amdahl speedup is the 1-node run of the same
algorithm:
\[
S(N) = \frac{T(1)}{T(N)}.
\]
So the baseline is the 1-node MapReduce run, not the 2-node run and not a
separate sequential implementation.

For this dataset, the measured curve rises from 1 to 16 nodes and then plateaus. This perfectly validates the qualitative expectation behind
Amdahl's law:
\begin{itemize}[leftmargin=1.5em]
  \item first, parallel work dominates and speedup rises;
  \item then communication, synchronization, and the serial merge cost dominate;
  \item finally the system stops scaling and can even slow down.
\end{itemize}

At 64 nodes, the speedup actually regressed slightly, demonstrating that the communication overhead in the 64x64 fully-connected worker mesh (which requires 4,096 connections) outweighed any parallel execution gains for an 8-chunk dataset.

\begin{figure}[H]
\centering
\begin{minipage}[t]{0.49\linewidth}
  \centering
  \includegraphics[width=\linewidth]{amdahl_8.png}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\linewidth}
  \centering
  \includegraphics[width=\linewidth]{amdahl_8_loglog.png}
\end{minipage}
\caption{Measured speedup for the 8-chunk CC dataset under the disk-backed architecture. The left figure is the main
Amdahl view with linear speedup on the \(y\)-axis; the right figure shows the
same data on a log-log scale to emphasize the high-node-count collapse.}
\end{figure}

\subsection{Batch x Stream Processing}

In a direct comparison using the 64-file Go source dataset on 8 nodes, the custom Go MapReduce system completed the compute phase in \textbf{0.266 seconds}, whereas Kafka Streams took \textbf{10.095 seconds}. While Kafka's compute time is significantly higher on this small batch workload, this is an architectural trade-off: Kafka Streams persists every record to a replicated topic log (ensuring high durability and fault tolerance but paying a high per-record I/O cost). Conversely, our system is strictly batch-oriented and relies entirely on in-memory maps and local temporary disk buckets, which is extremely fast for small datasets but lacks data replication. 

Furthermore, in a separate 50-node benchmark, Kafka Streams completed the wordcount job reliably in 248.5 seconds, whereas the custom Go MapReduce system struggled to deploy at that scale due to SSH connection limits and extreme memory overhead. Kafka manages dynamic partitions inherently, making it far more robust for continuous processing at scale.

\section{Conclusion}

The project successfully implements a coherent distributed MapReduce system in
Go, together with a reusable deployment pipeline and a prepared Kafka
comparison path. The main technical success is the detailed MapReduce runtime:
worker build injection, deployment, HTTP coordination, slot-preserving recovery,
and pluggable jobs all work end to end.

For the report experiments, the validated maximum dataset processed was the
fixed 64-file corpus used in the strong-scaling sweep. The measured results show
that the system scales well only up to a modest number of nodes for wordcount.
The best compute time was obtained at 8 nodes; after that, the reduce phase
dominates and speedup collapses.

In short:
\begin{itemize}[leftmargin=1.5em]
  \item the project works and is technically complete as a single-stage
  MapReduce system;
  \item the cross-login validation still needs to be added
  before the report is fully final.
\end{itemize}

\end{document}
